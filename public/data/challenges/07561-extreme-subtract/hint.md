#### Hint 1: Arbitrary precision subtraction
**The goal:** Compute `M - S` for large positive numbers.

**The nudge:** Reverse digits, subtract column by column borrowing 10 when top digit is smaller than bottom digit.

#### Hint 1: Generating two-element combinations
**The goal:** Form combinations of keys from token arrays.

**The nudge:** Match `T[number]` against `T[number]` where key A is not key B: `${A} ${B}`.

#### Hint 1: Generating numeric range union
**The goal:** Create union of integers from `L` to `H` inclusive.

**The nudge:** Build up a counter tuple until it reaches `L`, then accumulate elements into a union until reaching `H`.

#### Hint 1: Inspecting the last digit
**The goal:** Check if integer `T` is odd.

**The nudge:** Stringify `${T}`. Extract last digit: ``${string}${'1' | '3' | '5' | '7' | '9'}`` extends ``${T}`` ? true : false.

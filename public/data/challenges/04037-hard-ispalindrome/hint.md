#### Hint 1: Comparing string with its reverse
**The goal:** Return true if string or number `T` is a palindrome.

**The nudge:** Convert `T` to string. Compare string directly with its reversed character form.

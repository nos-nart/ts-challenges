#### Hint 1: Adding binary numbers with carry bit
**The goal:** Add two binary strings and return sum as binary string.

**The nudge:** Reverse strings, add bit by bit keeping track of carry bit `0` or `1`.

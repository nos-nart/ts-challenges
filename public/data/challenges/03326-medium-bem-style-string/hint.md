#### Hint 1: Distributing over optional parts
**The goal:** Format BEM strings: `${B}__${E}--${M}`.

**The nudge:** Handle empty arrays by transforming `E[number]` and `M[number]` into formatted string unions or empty strings.

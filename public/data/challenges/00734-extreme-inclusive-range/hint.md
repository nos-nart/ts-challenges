#### Hint 1: Building inclusive integer range
**The goal:** Generate tuple of numbers from `Lower` to `Higher` inclusive.

**The nudge:** Use a counter tuple. If `Higher < Lower`, return `[]`. Accumulate numbers into tuple until reaching `Higher`.

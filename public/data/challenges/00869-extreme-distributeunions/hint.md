#### Hint 1: Distributing deeply nested unions
**The goal:** Expand nested unions in objects/tuples into a union of concrete objects.

**The nudge:** Recursively distribute every union field across the entire structure.

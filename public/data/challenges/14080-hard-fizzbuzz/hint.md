#### Hint 1: Generating FizzBuzz sequence
**The goal:** Return tuple of FizzBuzz strings from 1 to `N`.

**The nudge:** Increment counter from 1 to `N`. Test divisibility by 3 and 5 via modulo helpers.

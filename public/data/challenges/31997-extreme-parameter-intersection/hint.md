#### Hint 1: Intersecting parameter types across function overloads
**The goal:** Intersect corresponding argument types across a union of function signatures.

**The nudge:** Merge parameter lists element by element into intersection tuples.

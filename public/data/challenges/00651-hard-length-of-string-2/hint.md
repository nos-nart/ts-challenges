#### Hint 1: Fast string length calculation for large strings
**The goal:** Compute string length without hitting recursion depth limits.

**The nudge:** Chunk string into groups of 10 or 100 characters before counting.

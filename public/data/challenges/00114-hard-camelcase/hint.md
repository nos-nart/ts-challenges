#### Hint 1: Underscore to camelCase conversion
**The goal:** Convert snake_case strings to camelCase, preserving leading and trailing underscores where appropriate.

**The nudge:** Parse segments delimited by `_` and capitalize first letter of each subsequent segment.

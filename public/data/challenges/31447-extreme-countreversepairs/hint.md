#### Hint 1: Counting inversions in tuple
**The goal:** Count pairs `(i, j)` where `i < j` and `Arr[i] > Arr[j]`.

**The nudge:** For each element, count how many elements to its right are strictly smaller.

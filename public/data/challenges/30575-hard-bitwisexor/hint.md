#### Hint 1: Bitwise XOR on binary strings
**The goal:** Compute bitwise XOR of two binary strings.

**The nudge:** Pad strings to equal length, then XOR matching bit pairs: `1 ^ 1 = 0`, `0 ^ 0 = 0`, `1 ^ 0 = 1`.

#### Hint 1: Stripping all characters in a target set
**The goal:** Remove all characters from string `S` that appear in string `R`.

**The nudge:** Convert `R` to a union of characters. Traverse `S` character by character, dropping any character in that union.

#### Hint 1: Tuple counter race
**The goal:** Determine if number `T > U`.

**The nudge:** Build a tuple element by element. Whichever target number matches the tuple length first is the smaller number.

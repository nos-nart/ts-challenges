#### Hint 1: Finding common character prefixes
**The goal:** Return the longest shared initial substring across array of strings `T`.

**The nudge:** Compare first character of each string. If all match, append character to accumulator and shift strings forward by 1.

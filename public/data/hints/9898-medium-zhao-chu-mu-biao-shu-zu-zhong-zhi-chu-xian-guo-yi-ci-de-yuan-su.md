#### Hint 1: Finding unique array elements
**The goal:** Return elements from array `T` that appear exactly once.

**The nudge:** Check if current element appears anywhere in the rest of the array or seen elements. If unique, include it in the result.

#### Hint 1: Big integer addition using digit arrays
**The goal:** Add two arbitrarily large numbers represented as strings.

**The nudge:** Reverse digit strings. Add matching digit pairs column by column with carry bit, simulating column addition.

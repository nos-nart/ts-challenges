#### Hint 1: Comparing tuple lengths
**The goal:** Compare lengths of arrays `A` and `B`, returning `1` if `A > B`, `-1` if `A < B`, and `0` if equal.

**The nudge:** Deconstruct one element from each array simultaneously until one or both become empty.

#### Hint 1: Counting element frequencies
**The goal:** Return an object mapping each element in tuple `T` to its occurrence count.

**The nudge:** Flatten array and accumulate occurrences into an object: `Count<T, Acc = {}>`.

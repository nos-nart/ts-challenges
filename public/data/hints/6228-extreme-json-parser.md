#### Hint 1: Complete JSON string parser
**The goal:** Parse valid JSON string literal into corresponding TypeScript type.

**The nudge:** Implement tokenization for strings, numbers, booleans, null, arrays, and objects, handling nested brackets and key-value colons.

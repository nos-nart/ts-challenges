#### Hint 1: Positional replacement with index counter
**The goal:** Fill elements in array `T` with value `N` from index `Start` to `End`.

**The nudge:** Use a counter tuple `Count['length']` to track current index and whether the active index falls within the `[Start, End)` range.

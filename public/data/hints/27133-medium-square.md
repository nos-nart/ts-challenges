#### Hint 1: Computing square of a number
**The goal:** Calculate `N * N`.

**The nudge:** Use multiplication via tuple length expansion: create `N` tuples of length `N` and measure the flattened length.

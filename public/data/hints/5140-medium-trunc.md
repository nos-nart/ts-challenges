#### Hint 1: Truncating decimal numbers
**The goal:** Return integer part of a number or string literal.

**The nudge:** Convert to string `${T}`. Match ``${infer Int}.${string}`` or return ``${T}``.

#### Hint 1: Comparing positive and negative integers
**The goal:** Compare two numbers `A` and `B`, returning `Comparison.Greater`, `Comparison.Lower`, or `Comparison.Equal`.

**The nudge:** Handle sign differences first (positive vs negative). For numbers with the same sign, compare digit string lengths, then compare digit by digit from left to right.

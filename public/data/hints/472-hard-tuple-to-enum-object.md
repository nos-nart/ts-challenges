#### Hint 1: Mapping tuple elements to enum keys and values
**The goal:** Convert tuple `["macOS", "Windows"]` into enum object `{ readonly MacOS: "macOS", readonly Windows: "Windows" }` (or index values).

**The nudge:** Map elements `T[number]` to capitalized keys and associate them with their string or index value.
